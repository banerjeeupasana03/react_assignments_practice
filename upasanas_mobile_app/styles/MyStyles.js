import {Stylesheet} from 'react-native';

var styles = {
  foo: {
    flex:1,
    justifyContent: "center",
    alignItem: "center",
    backgroundColor: "#ADD"
  },

  apptext: {
    color: "black",
    padding:10,
    backgroundColor: "white"
  }
};

export default styles;
